# Local video assets

Place the final cinematic MP4 files here:

- `hero-transformer.mp4`
- `route-corridor.mp4`

These files are intentionally not committed through the connector because Google Drive binary video download/upload is not available in this session. The `index.html` page should reference these local paths only:

```html
assets/video/hero-transformer.mp4
assets/video/route-corridor.mp4
```
